<% if @circus.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@circus.errors.count, "error") %> prohibited
        this circus from being saved:
      </h2>
      <ul>
        <% @circus.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

<%= form_for [@circus] do |f| %>
      <%= f.label :body %>
      <%= f.text_field :body %>
      <%= f.submit "Create Answer"%>
<% end %>
